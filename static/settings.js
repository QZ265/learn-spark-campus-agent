const $=selector=>document.querySelector(selector);
let settings=window.CampusStore.getSettings();
function fillForm(form,values){Object.entries(values).forEach(([key,value])=>{const field=form.elements[key];if(!field)return;if(field instanceof RadioNodeList){const option=Array.from(form.querySelectorAll(`[name="${key}"]`)).find(input=>input.value===String(value));if(option)option.checked=true;}else if(field.type==='checkbox')field.checked=Boolean(value);else field.value=value??'';});}
function valuesFromForm(form,base){const next={...base};Array.from(form.elements).forEach(field=>{if(!field.name)return;if(field.type==='radio'){if(field.checked)next[field.name]=field.value;}else if(field.type==='checkbox')next[field.name]=field.checked;else next[field.name]=field.value;});return next;}
function announce(message){const state=$('#settingsSaveState');state.textContent=message;state.classList.add('saved');setTimeout(()=>{state.textContent='更改会保存在当前浏览器';state.classList.remove('saved');},2200);}
document.querySelectorAll('[data-settings-tab]').forEach(button=>button.addEventListener('click',()=>{document.querySelectorAll('[data-settings-tab]').forEach(item=>item.classList.toggle('active',item===button));document.querySelectorAll('[data-settings-panel]').forEach(panel=>panel.classList.toggle('active',panel.dataset.settingsPanel===button.dataset.settingsTab));}));
fillForm($('#profileSettingsForm'),settings.profile);fillForm($('#appearanceSettingsForm'),settings.appearance);fillForm($('#aiSettingsForm'),settings.ai);
$('#profileSettingsForm').addEventListener('submit',event=>{event.preventDefault();settings.profile=valuesFromForm(event.currentTarget,settings.profile);settings=window.CampusStore.saveSettings(settings);announce('基本资料已保存');});
$('#appearanceSettingsForm').addEventListener('change',event=>{settings.appearance=valuesFromForm(event.currentTarget,settings.appearance);window.CampusStore.applyAppearance(settings.appearance);});
$('#appearanceSettingsForm').addEventListener('submit',event=>{event.preventDefault();settings.appearance=valuesFromForm(event.currentTarget,settings.appearance);settings=window.CampusStore.saveSettings(settings);announce('外观设置已保存');});
$('#aiSettingsForm').addEventListener('submit',event=>{event.preventDefault();settings.ai=valuesFromForm(event.currentTarget,settings.ai);settings=window.CampusStore.saveSettings(settings);announce('回答偏好已保存');});
